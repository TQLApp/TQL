<UserControl x:Class="Launcher.App.SearchResultUserControl"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Launcher.App"
             xmlns:support="clr-namespace:Launcher.App.Support"
             Background="Transparent"
             DataContextChanged="UserControl_DataContextChanged"
             MouseEnter="UserControl_MouseEnter"
             MouseLeave="UserControl_MouseLeave"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <support:MarqueeControl x:Name="_marquee">
            <StackPanel x:Name="_resultPanel"
                        Orientation="Horizontal"
                        Margin="2" />
        </support:MarqueeControl>
       
        <StackPanel x:Name="_iconsPanel"
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Margin="0,0,6,0" />
    </Grid>
</UserControl>
