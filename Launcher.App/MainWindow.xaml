<support:BlurWindow
    x:Class="Launcher.App.MainWindow"
    x:ClassModifier="internal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Launcher.App"
    xmlns:support="clr-namespace:Launcher.App.Support"
    mc:Ignorable="d"
    Title="Launcher"
    Width="800"
    SizeToContent="Height"
    ShowInTaskbar="False"
    Loaded="Window_Loaded"
    Closed="Window_Closed"
    PreviewKeyDown="Window_PreviewKeyDown"
    Deactivated="Window_Deactivated"
    Background="Transparent">
    <StackPanel Orientation="Vertical">
        <TextBox x:Name="_search" FontSize="18pt" Margin="14" Foreground="White" Background="Transparent" BorderThickness="0" TextChanged="_search_TextChanged">
            <TextBox.CaretBrush>
                <SolidColorBrush Color="Silver"/>
            </TextBox.CaretBrush>
        </TextBox>
        <ListBox x:Name="_results" SelectionMode="Single" Height="300">
            <ListBoxItem>
                <ListBoxItem.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Boards.png" Height="28" Margin="0,0,4,0" VerticalAlignment="Center" />
                        <TextBlock FontSize="18pt"  VerticalAlignment="Center">
                            <TextBlock.Resources>
                                <VisualBrush x:Key="WavyBrush" Viewbox="0,0,2,2" ViewboxUnits="Absolute" Viewport="0,2.3,6,4" ViewportUnits="Absolute" TileMode="Tile">
                                    <VisualBrush.Visual>
                                        <Path Data="M 0,1 C 1,0 1,2 2,1" Stroke="Red" StrokeThickness="0.2" StrokeEndLineCap="Square" StrokeStartLineCap="Square" />
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </TextBlock.Resources>
                            <Run Text="Hi, this is some wavy text">
                                <Run.TextDecorations>
                                    <TextDecoration Location="Underline">
                                        <TextDecoration.Pen>
                                            <Pen Brush="{StaticResource WavyBrush}" Thickness="6" />
                                        </TextDecoration.Pen>
                                    </TextDecoration>
                                </Run.TextDecorations>
                                </Run>
                        </TextBlock>
                    </StackPanel>
                </ListBoxItem.Content>
            </ListBoxItem>
        </ListBox>
    </StackPanel>
</support:BlurWindow>
